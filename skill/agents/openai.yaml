interface:
  display_name: "Ralph Workflows"
  short_description: "Build resumable autonomous coding loops"
  default_prompt: "Use $ralph-workflows to initialize, plan, inspect, or review this resumable autonomous sprint loop."
policy:
  allow_implicit_invocation: true
